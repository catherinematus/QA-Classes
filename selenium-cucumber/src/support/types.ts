export enum PAGES {
    HOME = "Home",
    HANDBOOK = "Handbook"
}

export enum SELECTOR_TYPES {
    CSS = "css",
    XPATH = "xpath",
    NAME = "name"
}

export enum SCROLL_DIRECTIONS {
    HEADER = "header",
    NAVIGATION_LINKS = "navigation links",
}
